# M4.5 / M4.5.1 / M4.5.2 / M4.5.3 / M4.5.4 / M4.5.5 / M4.5.7 / M4.5.8 / M4.5.9 / M4.5.10 / M5.1 / M5.2 / M5.3 / M5.4 单会话连续监听与未读队列受控切换说明

## 1. 阶段目标

M4.5 在 M4 单次自动回复已经可用的基础上，增加“当前微信会话连续监听”能力。M4.5.1 进一步把“最新客户消息”升级为“待回复客户消息组”。M4.5.2 解决不同分辨率、微信最大化和 Windows 缩放变化时的布局误判问题：员工打开一个目标客户会话后，点击 RPA 客户端“开始连续监听”，客户端会按固定间隔截图、先运行分辨率自适应布局检测，再 OCR、解析视觉消息流；发现客户连续消息组并稳定后，复用 M4 的 AI 回复、风控、输入、审核倒计时、真实发送和发送后校验链路。

M4.5 自动回复链路仍只处理当前打开的单个微信会话，不处理好友申请，不发送欢迎语。M5.1 新增的未读队列扫描只读展示微信左侧可见数字未读候选，M5.2 在候选行上只读 OCR 会话名、摘要、时间和未读数字，M5.3 增加连续扫描稳定性预演，M5.4 增加员工显式触发的首个可切换候选受控切换；M5.4 只点击会话行并校验右侧标题，不输入、不发送、不调用 AI 回复。

## 1.1 当前状态

更新时间：2026-08-13。

- 已完成：当前单会话连续监听、视觉消息流解析、待回复客户消息组、OCR + VLM 复核、分辨率自适应布局、多屏窗口锁定、性能耗时日志、视觉缓存、全消息 VLM 复核、客户气泡宽裁剪、表情占位、发送模式收口、InputOnly 安全干跑闭环、M5.1 可见数字未读会话只读队列、M5.2 候选行 OCR 可读队列展示、M5.3 候选稳定性只读预演和 M5.4 首个稳定候选受控切换。
- 正在进行：真实微信连续监听稳定性验收，以及 M5.4 点击后右侧聊天标题 OCR、左侧选中态回退与目标会话名匹配准确性核对。
- 接下来：继续根据 `vision-ocr-captures`、`debug-captures`、`unread-queue-captures`、`UnreadSwitchTitle` 和 `UnreadSwitchSelectedRow` 截图修正气泡裁剪、消息合并、重复文本过滤、VLM prompt、数字未读角标候选阈值、候选行 OCR 裁剪区域、稳定性阈值和点击后标题/选中态校验区域；M5.4 稳定后再进入 M5.5 点击后最新消息摘要一致性校验。
- 最终目标：在有人值守场景下，让员工打开一个微信会话后，RPA 能持续识别客户新消息，调用后端 AI 和风控，低风险时按安全发送模式完成输入/发送/日志闭环；同时展示左侧可见数字未读候选及其会话名、摘要、时间、未读数字和预演状态，并允许员工显式触发受控切换，为后续多会话队列提供观察和安全切换数据。

## 2. 启动方式

当前调试配置：

```json
{
  "ClientVersion": "0.4.5.5-m455",
  "Automation": {
    "EnableContinuousReply": true,
    "ContinuousPollIntervalSeconds": 3,
    "MessageMergeWindowSeconds": 5,
    "MaxContinuousSessionMinutes": 30,
    "MaxRepliesPerContinuousSession": 20,
    "MaxConsecutiveContinuousFailures": 3,
    "DuplicateMessageSuppressMinutes": 10,
    "ContinuousStartMode": "VisualLatestMessage",
    "ReplyGroupingMode": "Combined",
    "StopContinuousOnManualReviewRequired": true,
    "StopContinuousOnSendFailure": true,
    "EnablePerformanceDiagnostics": true,
    "ContinuousMaxVisualMessagesToOcr": 8,
    "ContinuousVisualOcrBottomRatio": 0.60,
    "ContinuousVisionReviewScope": "AllRecognizedMessages",
    "EnableContinuousLayoutCache": true
  }
}
```

如果本地配置中 `EnableContinuousReply=false`，需要改为 `true` 后才能点击“开始连续监听”。员工仍需先注册客户端、通过授权、打开官方微信 Windows 客户端并进入目标客户会话。

## 3. 执行流程

```text
员工进入目标客户会话
↓
点击“开始连续监听”
↓
截取完整聊天内容区并解析视觉消息列表
↓
判断最新有效消息发送方
↓
最新是客户则提取末尾连续客户消息组；最新是我方则等待新客户消息
↓
按 ContinuousPollIntervalSeconds 轮询当前会话
↓
发现新的待回复客户消息组
↓
等待 MessageMergeWindowSeconds 合并窗口
↓
消息组稳定后触发一轮 M4 单次回复闭环
↓
发送成功后记录消息指纹和回复次数
↓
继续监听当前会话
```

启动阶段会先解析当前可见聊天内容区的消息列表：如果最新有效消息是客户消息，会提取末尾连续客户消息组并立即复用 M4 单次闭环回复这一组问题；如果最新有效消息是我方消息，则认为当前对话已经回复到最新位置，只等待客户后续新消息。

## 4. 去重与合并

- `ChatMessageVisualExtractor` 会从完整聊天内容区识别消息气泡，输出带发送方的消息列表。
- 左侧浅色气泡识别为客户消息，右侧绿色气泡识别为我方消息，居中时间和系统提示识别为系统消息。
- 连续客户消息默认先逐条 OCR，再由 `CustomerMessageGroup` 做逻辑合并；不在 OCR 前把多条客户气泡合成一张大图。
- 调试模式下会额外保存每条气泡的 OCR 裁剪图到 `%LOCALAPPDATA%\AIChat\RpaClient\debug-captures`，文件名包含 `MessageBubble`。
- 最新有效消息按视觉位置从下往上判断，跳过系统消息；最新是客户才进入 AI 回复，最新是我方则等待下一条客户消息。
- M4.5.1 会从上到下扫描消息流，把末尾连续客户消息合并为 `CustomerMessageGroup`；遇到我方消息后，认为前面的客户消息组已被回复覆盖。
- `CustomerMessageExtractor` 会过滤微信系统提示，例如“你已添加了...现在可以开始聊天了”“通过了你的朋友验证请求”等，避免把系统通知当成客户问题。
- AI 的 `CustomerQuestion` 使用待回复客户消息组文本，`ConversationContext` 使用格式化后的消息列表，例如 `客户：...`、`我方：...`。
- 默认 `ReplyGroupingMode=Combined`，一组连续客户问题生成一条综合回复；本阶段不逐条连续发送。
- 连续监听默认不使用旧的左侧客户 OCR 作为回复判断依据；视觉消息流没有识别到有效消息时，本轮等待或停止人工确认。
- `ContinuousConversationState` 会保存已回复消息指纹和最近已回复消息文本。
- 同一组客户消息指纹不会重复回复。
- `DuplicateMessageSuppressMinutes` 内相同客户消息文本不会重复回复。
- 客户短时间连续发送多条消息时，RPA 等待合并窗口稳定后只回复一次。

## 5. M4.5.3 OCR + VLM 视觉复核

M4.5.3 将连续监听中的 OCR 低置信处理改为“先复核、再决策”。`ChatMessageVisualExtractor` 在识别每条微信消息气泡时，先使用 Windows OCR / PaddleOCR；当识别结果低置信、为空、短文本异常、数字混入中文或发送方未知时，会调用 `VisionOcrReviewer` 把单条气泡截图交给 Ollama 视觉模型复核。

默认配置：

```json
{
  "EnableVisionOcrReview": true,
  "VisionOcrProvider": "Ollama",
  "VisionOcrBaseUrl": "http://localhost:11434",
  "VisionOcrModel": "qwen2.5vl:7b",
  "VisionReviewMode": "AlwaysForCustomerMessages",
  "VisionOcrFailureBehavior": "SkipAndContinue",
  "StopContinuousOnManualReviewRequired": false
}
```

连续监听中，如果 VLM 仍无法确认文本或发送方，系统会跳过当前可疑消息并继续监听，不再直接因为 `OcrLowConfidence` 停止。AI 风控仍保留：`ShouldAutoSend=false` 或 `RiskLevel` 非 `Low` 时不发送；默认记录当前消息指纹，避免反复处理同一条消息。

VLM 调试气泡截图保存到：

```text
%LOCALAPPDATA%\AIChat\RpaClient\vision-ocr-captures
```

## 5.1 M4.5.4 多屏窗口锁定与监听目标可视化

当前 RPA 并不是按“第几个屏幕”监听，而是先通过 `WeChatWindowLocator` 按窗口标题定位微信窗口，再把该窗口客户区转换为 Windows 屏幕绝对坐标。微信窗口在哪个显示器上，截图、OCR、鼠标点击和键盘输入就会落在哪个显示器的坐标区域。

M4.5.4 已接入窗口锁定，用于解决多屏、多微信窗口或窗口焦点变化时的误选风险：

- 点击“开始任务”或“开始连续监听”时，锁定本次选中的微信窗口句柄、窗口标题、客户区坐标、显示器边界和 DPI。
- 连续监听过程中优先通过已锁定句柄复用目标窗口，不再每轮只按标题重新选择面积最大的微信窗口。
- 每轮识别前校验锁定窗口仍存在且可见，句柄、标题、显示器、DPI 和客户区坐标仍匹配；校验失败时停止连续监听，不继续发送。
- 单次闭环在输入前和发送前再次校验锁定窗口，避免 AI 生成或审核倒计时期间窗口被切走后继续点击。
- RPA 客户端 UI 显示当前锁定目标：窗口标题、窗口句柄、客户区坐标、显示器边界和 DPI。
- 运行日志记录监听目标，例如 `Handle=0x123456, Monitor=X=3840,Y=0,W=3840,H=2160, Client=X=3840,Y=0,W=3840,H=2088`，便于员工确认当前到底监听哪块屏幕。

新增配置：

```json
{
  "EnableWindowTargetLock": true,
  "WindowTargetLockClientBoundsTolerancePixels": 8
}
```

当标题关键字为“微信”时，窗口匹配会排除“企业微信”，避免联调时误锁企业微信窗口；如果同一桌面仍存在多个官方微信窗口，当前版本会在启动时锁定匹配分最高且客户区最大的窗口，后续可继续增加员工手动选择目标窗口。

## 5.2 M4.5.5 性能诊断与加速

M4.5.5 用于解决连续监听启动和 OCR 客户消息识别过慢的问题。当前版本在不降低发送安全门的前提下，减少重复识别并输出细分耗时日志。

新增配置：

```json
{
  "EnablePerformanceDiagnostics": true,
  "ContinuousMaxVisualMessagesToOcr": 8,
  "ContinuousVisualOcrBottomRatio": 0.60,
  "ContinuousVisionReviewScope": "AllRecognizedMessages",
  "EnableContinuousLayoutCache": true,
  "EnableContinuousVisualCache": true,
  "ContinuousVisualCacheMaxEntries": 80,
  "EnableContinuousUnchangedFrameSkip": true,
  "ContinuousUnchangedFrameBottomRatio": 0.75,
  "ContinuousDebugCaptureMode": "Always",
  "ContinuousReviewLatestSelfMessage": true
}
```

执行优化：

- UI 阶段显示更细：正在定位窗口、正在布局检测、布局缓存命中、正在识别底部最近气泡、正在 OCR 气泡、正在 VLM 复核气泡、正在生成 AI 回复。
- 连续监听只优先 OCR 聊天区底部最近的候选气泡，默认最多 8 个，不再每轮 OCR 整屏所有历史气泡。
- 连续监听 OCR 队列会跳过居中的时间 / 系统分割线，以及左右两侧头像或图标类非文字块，避免无效候选拖慢识别。
- VLM 当前真实测试复核范围为 `AllRecognizedMessages`；本轮识别到的非系统气泡都会复核一次，后续同一气泡通过缓存复用，不再重复调用 VLM。
- 当高置信 OCR 文本里出现中文句子混入 `/`、`\`、`|`、`_`、`—`、`–` 等异常符号时，也会判定为可疑并触发 VLM 复核，避免把“查询一下”识别成“查询—下”后直接进入 AI。
- 连续监听轮询时，如果微信窗口客户区尺寸不变，会复用上一次可用布局结果，跳过布局检测。
- 连续监听发现待回复消息后，会把已识别的窗口、布局和视觉消息流传给单次回复闭环，避免“先监听识别一遍、再回复又识别一遍”。
- 日志会输出 `[性能]` 前缀，包含窗口定位、布局检测、聊天区截图、气泡候选检测、每条 OCR、每次 VLM、AI 回复接口、调试截图保存等耗时。
- 合并窗口不再每秒重复 OCR。当前策略是先等待 `MessageMergeWindowSeconds`，倒计时结束后复查一次；如果消息组变化，再重新等待一个合并窗口。

## 5.3 M4.5.7 连续监听识别加速

M4.5.7 在不降低 `VisionReviewMode=AlwaysForCustomerMessages` 准确性策略的前提下，继续减少重复识别：

- 连续监听每轮会计算聊天区底部画面指纹；如果底部画面与上一轮完全一致，直接复用上一轮视觉消息流结果，跳过气泡检测、OCR 和 VLM。
- 每个气泡截图会计算 hash，并按“截图 hash + 发送方候选 + 气泡尺寸”缓存 OCR/VLM 合并结果；同一屏历史气泡不再每轮重复 OCR。
- VLM 二次复核复用第一次 OCR 的 `OcrResult` 和同一张气泡截图，不再为了复核重新跑一次 OCR。
- 最新有效消息为我方时，默认也会对这一条我方消息做一次 VLM 复核，用于提升运行日志和后续聊天记录清洗准确性；业务判断仍然是“最新为我方则不回复，等待客户新消息”。
- 当前真实测试将 `ContinuousVisionReviewScope` 调整为 `AllRecognizedMessages`：本轮识别到的非系统气泡都会执行一次 VLM 复核；后续同一气泡命中缓存后不再重复调用 VLM。
- 左侧客户文字候选会向客户消息可用宽度扩展后再截图，避免长句只截到文字外接框的一小段，导致 VLM 只能读到“最后半句”。
- 缓存只在一次“开始连续监听”会话内有效；窗口句柄、客户区或聊天区变化时会清空缓存。
- 当前真实调试阶段使用 `ContinuousDebugCaptureMode=Always`，每轮会保存气泡截图和 VLM 复核截图，便于排查识别偏差；识别稳定后可改回 `OnError` 减少磁盘写入。

验收时重点看运行日志：无新消息时应出现“聊天区底部画面未变化，跳过气泡检测、OCR 和 VLM”；有新消息时应继续识别最新待回复客户消息组，并对本轮识别到的非系统气泡执行 VLM 复核或缓存命中。

## 5.4 M4.5.8 全消息 VLM 复核与客户气泡宽裁剪

M4.5.8 是针对真实微信长句识别问题的小升级。现象是客户发送一条较长消息时，OCR/VLM 调试截图只截到了文字外接框的一部分，例如只能看到“尽量做傻瓜操作...”而看不到前后完整句子。此时即使 VLM 正常工作，也无法还原没有进入截图的内容。

当前处理策略：

- `ContinuousVisionReviewScope=AllRecognizedMessages` 时，本轮已识别的非系统消息气泡都会强制 VLM 复核。
- 气泡 hash 缓存继续生效，同一气泡不会在每轮连续监听中重复调用 VLM。
- 客户左侧文字候选会扩展到更宽的客户消息区域后再交给 OCR/VLM，优先保证长句和多段问题完整进入截图。
- VLM prompt 明确要求按自然阅读顺序返回所有可见行，不摘要、不改写、不省略。
- 如果微信把 `[机智]` 等文本转换成了内联表情图片，普通 OCR 无法直接读取原始文本；VLM 会尝试把明显的表情输出为 `[机智]`，无法确定时输出 `[表情]`。

- 客户候选完成宽裁剪后会再次合并同一行重叠候选，避免同一条长句被拆成“完整句 + 后半句”两个气泡。
- 消息流进入 `CustomerMessageGroup` 前会过滤同一客户气泡的包含式重复文本，只保留更完整或更高置信的那条消息。

如果后续性能压力较大，可把 `ContinuousVisionReviewScope` 调回 `PendingCustomerGroupOnly`，保留宽裁剪和缓存机制。

## 5.5 M4.5.10 InputOnly 安全干跑闭环

M4.5.10 用于收口默认联调模式的误发风险。默认配置为：

```json
{
  "SendMode": "InputOnly",
  "InputOnlyAfterVerifyAction": "ClearInput",
  "InputVerifyRetryCount": 1,
  "InputVerifyDelayMs": 300,
  "EnableKeyboardFallbackOnClipboardFailure": true
}
```

`InputOnly` 仍会点击微信输入框、输入 AI 回复并执行输入框 OCR 校验，但不会点击发送按钮。输入校验通过后按 `InputOnlyAfterVerifyAction` 做后处理：

- `ClearInput`：默认值，执行 `Ctrl+A` + `Backspace` 清空输入框，并再次 OCR 复核输入框已清空。
- `KeepDraft`：保留输入框草稿，便于人工检查，但存在误按发送风险，仅用于人工调试。
- `SelectAllOnly`：全选草稿但不删除，便于人工接管。

日志会记录 `ReplySendSkipped`、`InputOnlyClearStarted`、`InputOnlyClearVerifyOcr`、`InputOnlyCleared`；如果输入框 OCR 校验为空或未命中回复，RPA 会重新激活锁定微信窗口、清空输入框并按 `InputVerifyRetryCount` 重试输入。剪贴板被其它进程占用导致 `ClipboardPaste` 失败时，默认退回逐字符键盘输入。重试后仍失败时，`ClearInput` 会继续尝试清空可能残留的草稿并记录 `InputOnlyClearedAfterVerifyFailure`。如果清空后仍识别到非空文本，则任务失败并停止连续监听。

## 6. 安全停止

以下情况会停止连续监听并提示员工处理：

- 员工点击暂停或紧急停止。
- 授权失效或心跳返回不可继续运行。
- 找不到微信窗口或布局定位失败。
- OCR 可疑且 VLM 复核仍无法确认时，默认跳过本轮并继续监听；只有配置为停止策略时才停止。
- 最新有效消息发送方无法可靠判断且 VLM 也无法确认时，默认跳过本轮并继续监听。
- 布局候选分数不足、聊天区和输入区重叠、输入校验区越界或输入区高度异常。
- AI 返回转人工、高风险或不允许自动发送。
- 输入框校验失败。
- `SendMode=RealSendTest` 或 `ProductionGuarded` 真实发送后输入框未清空，或输入校验区 OCR 仍识别到明显非空文本。
- 达到最大连续监听时长、最大回复次数或连续失败次数。

M4.5 不降低真实发送准入：真实点击发送必须满足 `SendMode=RealSendTest` 或 `ProductionGuarded`、`ShouldAutoSend=true`、`RiskLevel=Low`、输入框 OCR 校验通过、审核倒计时结束、发送后校验通过。默认 `SendMode=InputOnly` 只输入并校验回复，不点击发送，并在 `InputOnlyAfterVerifyAction=ClearInput` 下清空输入框。

M4.5.2 中，连续监听每一轮都会复用 `WeChatLayoutDetector` 2.0：

- 从微信客户区底部向上生成多个输入区上边界候选。
- 使用长横线覆盖率、输入区白底比例、输入区高度、发送按钮位置、聊天区有效高度和消息气泡数量进行评分。
- 只把最终安全候选的 `ConversationContextRegion` 交给 `ChatMessageVisualExtractor`，避免把输入框内容或左侧会话列表混入客户消息流。
- `CoordinateMode=AutoOnly` 时，如果最佳候选低于 `LayoutDetectionMinConfidence`，本轮直接停止并提示员工，不使用配置坐标继续发送。

## 7. 输入模式

当前真实微信测试默认使用：

```json
{
  "InputMode": "ClipboardPaste"
}
```

原因是逐字 Unicode 键盘输入在部分微信 / 输入法环境下可能出现短回复丢字或错字，例如后端 AI 回复为“好的，可以的。”，微信输入框中可能变成“好的，，以的。”。`ClipboardPaste` 仍然只操作官方微信前台窗口：先点击输入框，再写入剪贴板并模拟 `Ctrl+V`，随后继续做输入框 OCR 校验、审核倒计时和发送后校验。

如果需要继续验证逐字键盘输入，可临时改回 `KeyboardTyping`，但应重点观察短中文回复是否出现漏字。

## 8. 代码入口

- `SingleConversationReplyCycleExecutor`：可复用的单次回复闭环。
- `SingleConversationTaskRunner`：兼容原来的“开始任务”单次入口。
- `ContinuousConversationTaskRunner`：连续监听编排。
- `ChatMessageVisualExtractor`：视觉消息流解析，输出客户 / 我方 / 系统 / 未知消息列表。
- `CustomerMessageGroup`：待回复客户连续消息组，保存整组问题文本、上下文和指纹。
- `CustomerMessageExtractor`：客户消息快照、上下文和指纹生成。
- `ContinuousConversationState`：去重、回复次数和连续失败状态。

## 9. 验收建议

1. 使用测试微信号和测试客户会话。
2. 设置 `EnableContinuousReply=true`。
3. 启动前准备一条客户未回复消息，点击“开始连续监听”后确认会立即回复一次。
4. 对同一条客户消息先完成一次回复，再重新点击“开始连续监听”，确认最新消息发送方显示为我方且不会重复回复旧消息。
5. 让客户发送一条新消息，确认只回复一次。
6. 让客户连续发送“您好 / 你是谁？ / 你能帮我做什么？”，确认合并窗口后生成一条综合回复，且覆盖三句内容。
7. 发送成功后继续发送下一条新消息，确认连续监听继续工作。
8. 测试高风险问题、AI 转人工、暂停和紧急停止，确认不会继续发送。
