# M2 设备与任务基础模块说明

## 1. 本轮范围

M2 已打通以下基础闭环：

- 后端接入 EF Core + PostgreSQL。
- 员工、员工客户端授权、微信工作号、物理主机、Windows VM、RPA 客户端实例、RPA 任务、RPA 动作日志建模。
- 管理后台提供 M2 资源列表与员工客户端授权配置骨架。
- RPA 客户端提供 API 地址配置、注册、心跳、授权状态展示和“开始任务”禁用控制。
- 本轮不实现真实 OCR、真实微信窗口识别、真实鼠标键盘操作和真实 AI 调用。

## 2. 数据库配置

运行时连接字符串只读取环境变量：

```powershell
$env:ConnectionStrings__DefaultConnection="Host=localhost;Port=5432;Database=aichat;Username=postgres;Password=YOUR_PASSWORD"
```

仓库提供：

- `.env.example`：本地环境变量模板，不包含真实密码。
- `docker-compose.postgres.yml`：可选 PostgreSQL 本地复现环境。

如使用项目提供的 compose 模板：

```powershell
docker compose -f docker-compose.postgres.yml --env-file .env up -d
```

## 3. 后端 API

管理后台基础接口：

- `GET/POST/PUT /api/employees`
- `GET/PUT /api/employees/{id}/client-access`
- `GET/POST/PUT /api/wechat-accounts`
- `GET/POST/PUT /api/device-hosts`
- `GET/POST/PUT /api/virtual-devices`
- `GET /api/rpa-client-instances`
- `GET /api/rpa-tasks`
- `GET /api/rpa-action-logs`

RPA Agent 接口：

- `POST /api/agent/register`
- `POST /api/agent/heartbeat`
- `GET /api/agent/access-policy`
- `POST /api/agent/tasks`
- `PUT /api/agent/tasks/{id}/status`
- `PUT /api/agent/tasks/{id}/result`
- `POST /api/agent/tasks/{id}/action-logs`

## 4. 授权规则

员工客户端授权由 `EmployeeClientAccessPolicy` 控制：

- `Enabled`：授权有效时允许开始和继续任务。
- `Paused`：暂停时 RPA 客户端不可开始任务，运行中心跳会返回不可继续。
- `Disabled`：禁用时 RPA 客户端不可开始任务。
- `ValidFromUtc` / `ValidToUtc`：不在有效期内不可开始任务。
- `MaxDailyUsageMinutes`：预留每日最长使用时长控制。
- `MaxSessionMinutes`：单次会话超限后心跳返回不可继续。

RPA 客户端收到不可继续状态后，会禁用“开始任务”，并把当前任务占位状态置为停止。

## 5. 前端与客户端配置

Web 管理后台：

- 默认同源请求 `/api`。
- 可通过 `VITE_API_BASE_URL` 指定后端地址。

RPA 客户端：

- 默认读取 `src/AIChat.RpaClient/appsettings.json`。
- 支持环境变量前缀 `AICHAT_RPA_` 覆盖，例如 `AICHAT_RPA_ApiBaseUrl`。
- 员工可在界面中填写 API 地址、员工 ID、VM ID、微信工作号 ID。

## 6. 验证命令

```powershell
dotnet build AIChat.slnx
dotnet test AIChat.slnx
npm run build --prefix src/AIChat.Web
dotnet ef migrations add InitialM2DeviceTaskSchema --project src/AIChat.Infrastructure --startup-project src/AIChat.Api --output-dir Persistence/Migrations
```

如果本机未安装 `dotnet-ef`，可先安装到本地工具目录后再执行迁移生成。
