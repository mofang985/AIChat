# M4.5.3 OCR + VLM 视觉复核说明

## 1. 阶段目标

M4.5.3 用于解决微信聊天气泡小字、短句、问号、数字混入等 OCR 不稳定问题。系统不再因为单次 OCR 低置信度直接停止连续监听或转人工，而是先用 Windows OCR / PaddleOCR 识别，再在结果可疑时调用 VLM 视觉模型复核气泡截图。

本阶段仍然只处理当前打开的单个微信会话，不进入 M5 多会话队列，不读取微信数据库，不使用协议、Hook、插件或非官方客户端。

当前状态更新：2026-08-07。M4.5.8 真实微信调试阶段已把连续监听 VLM 复核范围调整为 `AllRecognizedMessages`，即本轮识别到的非系统气泡都会执行一次 VLM 复核；同一气泡命中缓存后不重复调用。当前还增加了客户气泡宽裁剪、微信内联表情占位和重复文本过滤。

## 2. 核心流程

```text
微信聊天内容区截图
-> OpenCV 识别消息气泡
-> 每个气泡单独裁剪
-> Windows OCR / PaddleOCR 识别
-> 判断 OCR 是否可疑
-> 可疑时调用 VLM 复核气泡截图
-> 合并 OCR 与 VLM 结果
-> 生成客户 / 我方 / 系统 / 未知消息列表
-> 提取待回复客户消息组
-> 调用后端 AI 回复建议
-> 风控、输入校验、倒计时、真实发送、发送后校验
```

VLM 只用于“读图复核文字和发送方”，不生成客服回复。客服回复仍由后端 `/api/ai/reply-suggestions` 生成并经过原有风控。

## 3. 配置项

配置位置：`src/AIChat.RpaClient/appsettings.json`

```json
{
  "Automation": {
    "EnableVisionOcrReview": true,
    "VisionOcrProvider": "Ollama",
    "VisionOcrBaseUrl": "http://localhost:11434",
    "VisionOcrModel": "qwen2.5vl:7b",
    "VisionReviewMode": "AlwaysForCustomerMessages",
    "VisionOcrTimeoutSeconds": 8,
    "VisionOcrMinConfidence": 0.70,
    "VisionOcrFailureBehavior": "SkipAndContinue",
    "EnableVisionOcrDebugCaptures": true,
    "VisionOcrDebugCaptureDirectory": "",
    "ContinuousVisionReviewScope": "AllRecognizedMessages",
    "ContinuousDebugCaptureMode": "Always",
    "StopContinuousOnManualReviewRequired": false
  }
}
```

说明：

- `AlwaysForCustomerMessages`：客户消息优先触发 VLM 复核。
- `ContinuousVisionReviewScope=AllRecognizedMessages`：当前真实微信识别调试值。本轮识别到的非系统气泡都会做一次 VLM 复核；同一气泡依靠 hash 缓存避免每轮重复调用。
- `SuspiciousOnly`：后续性能优化可选值。只在 OCR 低置信、空文本、短文本异常、数字混入中文、中文句子混入异常符号或发送方未知时调用 VLM。
- `SkipAndContinue`：VLM 失败时跳过本轮可疑消息，连续监听继续等待下一条客户消息。
- `UseOcrFallback`：VLM 失败时回退 OCR 文本，不建议真实发送测试默认启用。

## 4. Ollama 模型部署

本机测试：

```powershell
ollama pull qwen2.5vl:7b
ollama serve
```

如果机器配置较低，可以先测试：

```powershell
ollama pull qwen2.5vl:3b
```

然后把 `VisionOcrModel` 改为 `qwen2.5vl:3b`。

后期上线建议：

- 小规模测试：每台 VM 或测试机本地运行 Ollama。
- 节省资源：一台局域网高配主机运行 Ollama/VLM 服务，多个 RPA 客户端把 `VisionOcrBaseUrl` 指向该主机。
- 规模扩大：可替换为 vLLM / SGLang 等统一视觉模型服务，RPA 侧只调整 Provider/BaseUrl/Model 配置。

开源模型通常没有按次调用费用，但仍有硬件、电费、显存和维护成本。正式商用前需要单独核对模型许可证与商用约束。

## 5. 安全规则

- VLM 输入只传单条消息气泡截图或局部上下文截图，不上传完整微信窗口截图。
- VLM 结果只能修正 OCR 文字和发送方，不绕过后端 AI 风控。
- 微信内联表情如果已经被客户端渲染为图片，普通 OCR 无法还原原始输入文本；VLM 会尝试输出 `[机智]` 等占位符，无法确认时输出 `[表情]`。
- VLM 未确认时不发送，仅跳过本轮并继续监听。
- 默认 `SendMode=InputOnly` 不点击发送，只输入、校验并清空草稿。
- 真实发送仍必须显式切换为 `SendMode=RealSendTest` 或 `ProductionGuarded`，并同时满足：最新待回复消息确认为客户消息、AI `ShouldAutoSend=true`、`RiskLevel=Low`、输入框校验通过、发送前倒计时结束、发送后校验通过。

## 6. 调试截图

消息气泡 OCR 调试截图：

```text
%LOCALAPPDATA%\AIChat\RpaClient\debug-captures
```

VLM 复核气泡截图：

```text
%LOCALAPPDATA%\AIChat\RpaClient\vision-ocr-captures
```

这些截图只保存在 RPA 客户端本机，不上传后端。

## 7. 验收重点

- OCR 把“你好”误识别为“1子”时，VLM 能修正为“你好”。
- OCR 在“你能帮我做什么？”末尾误识别出数字时，VLM 能复核正确文本。
- 最新消息发送方为未知时，VLM 能判断客户/我方/系统。
- Ollama 或 VLM 模型不可用时，连续监听不崩溃、不误发，跳过本轮继续等待。
- AI 不允许自动发送或风险非 Low 时不发送；默认记录当前消息指纹并继续监听新消息，不反复处理同一条消息。
